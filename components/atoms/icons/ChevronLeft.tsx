import {ArrowIosBackOutline as Icon} from '@styled-icons/evaicons-outline/ArrowIosBackOutline'
import styled from "styled-components";

const ChevronLeft = styled(Icon)(() => ({}))
export default ChevronLeft;